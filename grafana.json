fields @timestamp, message, `process.thread.name`, log.logger
| filter @logStream like /-tg-dp\//
| filter message like /Job exit status|ExitStatus|BatchStatus|COMPLETED|FAILED|STOPPED|Job Failure/
| sort @timestamp desc
| limit 100