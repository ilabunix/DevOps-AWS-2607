You can send:

I got more details from Eric on how DEV was set up. It was not just a straightforward pipeline deployment — there was manual Terraform sequencing, sections were commented out, resources were deployed in stages, IDs were captured and reused, and there is also certificate setup involved.

Based on that, I can’t confidently estimate this as a 1–2 hour effort right now. I need to review the Terraform and understand/fix the deployment flow first so we’re not repeating the same manual process in UAT.


You can send:

I discussed with Eric how the TaskTop environment was deployed in DEV. Based on what he shared, the deployment required manually commenting out Terraform sections/resources, deploying them in stages, capturing resource IDs, and then bringing additional components up afterward, with the ALB deployed last.

We really shouldn’t be deploying the environment that way going forward. The IaC should be handling the resource dependencies and passing the required outputs/inputs between modules so the environment can be deployed in a predictable and repeatable way without manually commenting sections in and out.

Before we move into UAT, I think we need to review the Terraform and understand why that sequencing was required in DEV and fix the dependency/module flow where needed. There is also certificate setup that needs to be accounted for.

Because of that, this is more than simply running the UAT pipeline. I’ll need to review the current Terraform/setup first, address or validate the deployment dependencies, and then we can determine the actual UAT deployment steps and effort.