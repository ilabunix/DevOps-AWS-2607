Quick update on TaskTop Terraform: I reviewed and remediated the undeclared module dependencies that were causing the DEV deployment to require manual sequencing/commenting resources in and out.

The dependencies between the IAM role/instance profile, instance profile/EC2, and ALB target group attachment have now been explicitly wired through Terraform rather than relying on name-based lookups/existing resources.

I pushed the changes to the repo and validated them in DEV. Terraform plan and apply both completed successfully, with DEV ultimately showing no infrastructure changes/drift.

This gives us much more confidence that UAT can be deployed through the pipeline in the correct dependency order without repeating the manual staged deployment used in DEV.

The remaining prerequisite is the UAT ACM certificate setup/import before the UAT deployment.