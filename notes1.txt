Proceed only with the one-alert TEST pilot. Add the variables and dynamic notification_settings block, but do not remove grafana_notification_policy yet. Run terraform fmt, terraform validate, and terraform plan. Stop before apply and show me the complete plan summary and any resource changes. The expected result is an in-place update to only the targeted grafana_rule_group, with no destroy/recreate and no unrelated alert-rule changes.